1. git clone https://github.com/JMazing35/IT350.git
2. git add -A
3. git commit -m "Statement about update."
4. git push origin master



5. Use JAVA, PHP, ASP.NET

For Admin Page: User can login, change password, add products, delete products, update product info, etc. 

 sudo apt-get update
 sudo apt-get upgrade

 sudo apt-get install mysql-server

 admin
 DMitch

go team go - 

 mysql -u root -p

 SHOW DATABASES;

 https://www.digitalocean.com/community/tutorials/a-basic-mysql-tutorial

VM Set up:

sudo apt-get update
sudo apt-get upgrade

sudo apt-get install apache2 php7.0 libapache2-mod-php7.0

sudo chmod -R 755 /var/www
sudo chmod -R 755 /var/www

sudo apt-get install git

sudo apt-get install mysql-server (go team go)


added [mysqld] at top of /etc/mysql/my.cnf

sudo service mysqld stop
sudo service mysqld start

sudo nano /etc/apache2/sites-enabled/000-default.conf 
 - change to /var/www/html to /var/www/IT350
 - clone repo into /var/www
 
 git clone https://github.com/JMazing35/IT350.git
 
 sudo service apache2 restart
 

